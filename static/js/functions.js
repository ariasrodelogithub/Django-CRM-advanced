function messageError(obj) {
    $.each(obj, function (key, value) {
        console.log('---------x---------------')
        console.log('LLAVE: ' + key)
        console.log('VALOR: ' + value)
        
    })
    
}